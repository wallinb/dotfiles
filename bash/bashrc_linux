eval "$(rbenv init -)"
eval "$(pyenv init -)"

export TERM=xterm-256color

export PATH="$HOME/.pyenv/bin:$PATH"
eval "$(pyenv init -)"
export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init -)"

set -o vi

# added by Anaconda 2.1.0 installer
export PATH="/home/bwallin/anaconda/bin:$PATH"
